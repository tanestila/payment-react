import { render } from "@testing-library/react";
import Creator from "./Creator";

describe("Merchant creator", () => {
  it("render", () => {
    // const { getByText } = render(<Creator />);
    // expect(getByText("Hello Worldd!")).not.toBeNull();
  });
});
